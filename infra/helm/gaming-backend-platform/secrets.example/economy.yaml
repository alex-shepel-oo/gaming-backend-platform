# Template only. The real Secret is never committed: copy this file,
# fill in real values, and apply it directly (kubectl apply -f) rather than
# keeping the filled-in copy anywhere under version control.
apiVersion: v1
kind: Secret
metadata:
  name: economy-secrets
  namespace: gaming-platform
type: Opaque
stringData:
  POSTGRES_PASSWORD: "change-me"
  ConnectionStrings__EconomyDb: "Host=economy-db.gaming-platform.svc.cluster.local;Port=5432;Database=economy_db;Username=economy;Password=change-me"
